rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to hunt media files
    match /hunt-media/{allPaths=**} {
      allow read, write: if true;
    }
  }
}